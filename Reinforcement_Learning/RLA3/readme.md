Running the file without arguments will perform the cartpole experiment.

To run the cartpole experiment:
python experiments.py cartpole

To run the acrobot and lunar lander experiments:
python experiments.py other_envs

These commands will perform the experiments, save the results and save the figures used in the report.